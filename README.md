# Directional 프론트엔드 코딩 테스트

## 프로젝트 개요

---

- 실행환경: node: v20.19.4
- 기술 스택
  - 프레임워크: React.js v19.2.0
    - 소규모 프로젝트로 Next.JS 불필요하다고 판단
  - 타입검사: TypeScript v5
  - 스타일: TailwindCSS v4 + DaisyUI v5
  - 유효성검사: react-hook-form + @hookform/resolvers + zod
  - 데이터시각화: recharts
  - 기타
    - 날짜 제어: date-fns, react-day-picker
    - 데이터 그리드: ag-grid-react
    - 상태 관리: zustand(무의식적으로, 설치하고 한 번도 안 씀)
- 주요 구현 기능
  - 로그인 / 로그아웃
  - 전역 모달
  - 게시판
    - 게시글 작성 / 조회 / 수정 / 삭제
      - 해시태그 입력 방법
        - 인풋에 입력 후 엔터. 공백과 컴마(,)로 구분해 해시태그 구분
        - 엔터 입력 시 찾아낸 해시태그들을 인풋 바로 위에 하늘색 뱃지 형태로 표시
        - 저장 시 뱃지로 표시되는 해시태그만 입력되고,<br/>태그 입력 인풋에 편집중인 문자열은 무시된다.
    - 무한스크롤
      - AG-Grid에서 지원하는 무한스크롤 기능에 맞춰 구현됨
        - 이전 페이지를 남겨둔 채 다음 페이지를 불러와 누적하는 방식
        - 이에 따라, prevCursor 불필요하다고 판단
    - 검색, 정렬, 필터
      - limit 값만 10으로 고정
      - 그 외 API 호출 시 사용되는 모든 게시글 목록 검색 파라미터 반영
  - 데이터 시각화
    - 아래 엔드포인트 구현 완료
      - /mock/top-coffee-brands → 바 차트, 도넛 차트 두 가지
      - /mock/popular-snack-brands → 바 차트, 도넛 차트 두 가지
      - /mock/weekly-mood-trend → 스택형 바 차트, 스택형 면적(Stacked Bar/Area) 차트 두 가지
      - /mock/weekly-workout-trend → 스택형 바 차트, 스택형 면적(Stacked Bar/Area) 차트 두 가지
- 디자인 패턴
  - `src` 아래 페이지별 MVVM 패턴 정의
    - `consts`: 상수 모음 [M]
    - `interfaces`: 모델 모음 [M]
      - 서버-프론트 사이드 타입 통일
      - 가끔 편의 상 type 사용
    - `schemas`: 유효성 검사 모델 모음 [M]
    - `components`: 페이지 구성요소를 여러 컴포넌트로 분리한 경우, 자식 컴포넌트가 위치 [V]
    - `styles`: CSS 모듈 스타일 모음 폴더 [V] - 본 프로젝트에서는 규모 상 생략
    - `hooks.ts` : 클라이언트 사이드에서 실행되는 공통 훅들이 위치 [VM]
    - `utils` : 공통으로 사용되는 주요 유틸 함수 등이 위치

---

## 실행 방법

- 아래 명령으로 실행
  ```shell
  npm run dev // 또는 yarn dev
  ```
- 로그인
  - 메일에서 공유된 계정 정보로 홈에서 로그인

---

## 미완성 항목

- Axios 인터셉트
  - 로그인 직후 토큰 유효기간 1일로 확인 → 로그인 / 로그아웃 기능 추가 결정
  - 리프레시 토큰 없이 접근 토큰 정보만 있는 상황 확인 → 인터셉터 코드 추가
    - 사이드 이펙트로 인해, 현재 주석으로 막은 상태입니다.
- 게시글 작성
  - 금칙어 필터: 기능은 작동하지만, 오류 메시지가 안 뜸니다.
- 데이터시각화
  - 멀티라인 차트(2종)
- 에러 표시
  - 콘솔에 error 메시지는 띄우지만 토스트나 모달을 띄워 표시하지 않습니다.

---

## 소감

지난 5개월 동안 C++과 파이썬만 다루다가 오랜만에 JS와 리액트를 써봤습니다.<br/>
덕분에, 프론트엔드 개발 감각을 되살리는데 많은 도움이 됐습니다.<br/><br/>
내일 일정도 있어, 필수 구현 기능을 다 구현하지 못한 것들이 많은데요.<br/>
마무리짓지 못한 멀티라인 차트이 많이 아쉽습니다.
